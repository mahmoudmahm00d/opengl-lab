// homework: a moving circle from left to right, should rotate around a fixed circle in the center, then continue its linear move after being at the right side of the fixed cirlce.

// solution:

void homework()
{
	// Note: build this code line by line, show the misbehavior then correct it, start with the moving circle passing through the fixed circle
	const float radius = 1;
	glTranslated(0, 0, -10);
	DrawCircle(0, 0, radius);
	if (cx < -radius * 2 || cx > radius * 2 || angle > 180) // no adjacent
	{
		cx++;
		DrawCircle(cx, 0, radius);
	}
	else
	{
		glRotated(angle, 0, 0, -1);
		angle++;
		DrawCircle(-radius * 2, 0, radius);
		cx = radius * 2;
	}
}